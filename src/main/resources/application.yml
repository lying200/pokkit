pokkit:
  provider: ${POKKIT_PROVIDER:openai}

spring:
  main:
    web-application-type: none
  profiles:
    active: ${POKKIT_PROVIDER:openai}
  ai:
    model:
      # 只激活选定的 provider，其余自动禁用
      chat: ${POKKIT_PROVIDER:openai}
      embedding: none
      image: none
      moderation: none
      audio:
        speech: none
        transcription: none
